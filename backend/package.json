{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend of the alert system",
  "main": "index.ts",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "dev": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "format": "prettier --write ."
  }
}
